/**
 * RBAC Constants
 *
 * Metadata keys for RBAC decorators.
 * Use these constants instead of raw strings across decorators and guards.
 */

export const IS_PUBLIC_KEY = 'is_public';
export const IS_AUTHENTICATED_KEY = 'is_authenticated';
export const REQUIRED_PERMISSIONS_KEY = 'required_permissions';
export const PERMISSION_MODE_KEY = 'permission_mode';